{
  "version": 2,
  "builds": [
    { "src": "api/*.py",  "use": "@vercel/python" },
    { "src": "app/**",    "use": "@vercel/static"  }
  ],
  "routes": [
    { "src": "/api/predict/(.*)", "dest": "/api/predict.py" },
    { "src": "/(.*)",             "dest": "/app/$1"          }
  ]
}